name: CI/CD

on:
  push:
    branches:
      - main
  workflow_dispatch:
  pull_request:

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version-file: ".nvmrc"
          cache: npm

      - run: npm ci

      - run: npm run lint

  deploy:
    concurrency: gh-pages-deployment

    needs:
      - lint

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    env:
      NEXT_TELEMETRY_DISABLED: "1"

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version-file: ".nvmrc"
          cache: npm

      - run: npm ci

      - name: Build site (preview)
        if: github.ref != 'refs/heads/main' && github.event_name == 'pull_request'
        run: npm run build
        env:
          BASE_PATH: /pr-previews/pr-${{ github.event.pull_request.number }}

      - name: Build site (production)
        if: github.ref == 'refs/heads/main'
        run: npm run build
        env:
          NEXT_PUBLIC_POSTHOG_KEY: ${{ secrets.NEXT_PUBLIC_POSTHOG_KEY }}
          NEXT_PUBLIC_POSTHOG_HOST: ${{ secrets.NEXT_PUBLIC_POSTHOG_HOST }}

      - name: Deploy (preview)
        if: github.ref != 'refs/heads/main'
        uses: rossjrw/pr-preview-action@v1
        with:
          source-dir: "./out"
          comment: false

      - name: Deploy (production)
        if: github.ref == 'refs/heads/main'
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: "./out"
          clean-exclude: pr-preview/
          force: true
          single-commit: true
          clean: true

      # We should use the official GitHub pages deployment, but the preview deployment feature is
      #  still in private beta. Therefore we use the hacky rossjrw/pr-preview-action@v1 action
      # - name: Setup Pages
      #   uses: actions/configure-pages@v5
      #
      # - name: Upload artifact
      #   uses: actions/upload-pages-artifact@v3
      #   with:
      #     path: "./out"
      #
      # - name: Deploy to GitHub Pages
      #   id: deployment
      #   uses: actions/deploy-pages@v4
