export const metadata = {
  title: "Setup",
  description: "How to setup your project to use Foundations.",
};

## Prerequisites

- A React project (preferably Next.js)
- [Tailwind CSS](https://tailwindcss.com/) installed and configured

> We are using Tailwind 4 (beta). If you are using an older version, adapt as needed.

## Installation

Install the core dependencies:

```bash
npm install cva@beta tailwind-merge
```

## CSS Setup

<Tabs>
  <TabsItems>
    <TabsItem>Automatic color scheme</TabsItem>
    <TabsItem>With theme switcher</TabsItem>
  </TabsItems>
  <TabsPanels>
    <TabsPanel>
      <SourceCode file="src/foundations/setup/globals-prefers-schema.css" />
    </TabsPanel>
    <TabsPanel>
      <SourceCode file="src/foundations/setup/globals-data-attr.css" />
    </TabsPanel>
  </TabsPanels>
</Tabs>

## Utilities Setup

Create a `lib/utils.ts` file with our utility functions:

<SourceCode file="src/lib/utils/index.ts" />

## Icons Setup

We use [Phosphor Icons](https://phosphoricons.com/) by default, but you can use any icon library of your choice:

```bash
npm install @phosphor-icons/react
```
