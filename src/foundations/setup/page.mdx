export const metadata = {
  title: "Setup",
  description: "How to setup your project to use Foundations.",
};

## Tailwind

Install and configure [Tailwind](https://tailwindcss.com/).

Make sure you have the following tokens:

<Tabs>
  <TabsItems>
    <TabsItem>Automatic color scheme</TabsItem>
    <TabsItem>With theme switcher</TabsItem>
  </TabsItems>
  <TabsPanels>
    <TabsPanel>
      <SourceCode file="src/foundations/setup/globals-prefers-schema.css" />
    </TabsPanel>
    <TabsPanel>
      <SourceCode file="src/foundations/setup/globals-data-attr.css" />
    </TabsPanel>
  </TabsPanels>
</Tabs>

## Icons

We are using [Phosphor Icons](https://phosphoricons.com/) for the icons. Feel free to use any other icon library and make the necessary changes in the codebase.

```bash
npm install @phosphor-icons/react
```

## Other dependencies

There are a few dependencies you need to install:

```bash
npm install cva@beta tailwind-merge
```

## Class utility

Create a `lib/utils.ts` file with the following code:

```tsx title="lib/utils.ts"
import { defineConfig } from "cva";
import { twMerge } from "tailwind-merge";

export const {
  cva,
  cx: cn,
  compose,
} = defineConfig({
  hooks: {
    onComplete: (className) => twMerge(className),
  },
});
```
