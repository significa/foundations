export const metadata = {
  title: "Portal",
  description:
    "A portal is a component that renders its children into a new DOM node.",
  files: ["src/foundations/ui/portal/portal.tsx"],
};

## API Reference

<PropsTable
  definition={{ container: { type: "Element", default: "document.body" } }}
/>
