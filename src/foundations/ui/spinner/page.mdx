export const metadata = {
  title: "Spinner",
  description: "A component to display a loading state.",
  files: ["src/foundations/ui/spinner/spinner.tsx"],
  preview: "spinner",
};

## API Reference

Extends the `div` element.

<PropsTable
  definition={{
    size: {
      type: ["xs", "sm", "md", "lg"],
      default: "md",
    },
  }}
/>

## Examples

### Sizes

<Preview slug="spinner-sizes" />

### Color

<Preview slug="spinner-color" />
