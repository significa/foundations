---
title: Input
description: A simple text input component.
preview: input
files:
  - src/foundations/ui/input/input.tsx
dependencies:
  - name: Slot
    href: /components/slot
  - name: composeRefs
    href: /utils/compose-refs

folder: UI
---

## Features

- **Multiple Variants**: Supports default and minimal styles
- **Validation States**: Built-in support for invalid state
- **Flexible Addons**: Support for prefix and suffix elements
- **Interactive Elements**: Optional interactive addons for enhanced functionality

## Anatomy

```tsx
<Input.Group>
  <Input.Addon />
  <Input />
  <Input.Addon />
</Input.Group>
```

`Input` is for text-like single-line controls: `text`, `email`, `password`, `search`, `tel`, `url`, `number`, `file`, and the date/time family (`date`, `time`, `datetime-local`, `month`, `week`). For the non-text-like input types, reach for the dedicated primitive: [Checkbox](/ui/checkbox), [Radio](/ui/radio), [Slider](/ui/slider) (for `range`), [ColorPicker](/ui/color-picker), [Switch](/ui/switch), [Toggle](/ui/toggle), [OTPInput](/ui/otp-input), or [DatePicker](/ui/date-picker) when you want a full picker UI rather than native entry.

## API Reference

### Input

Extends the `input` element.

<PropsTable
  definition={{
    variant: {
      type: ['"default"', '"minimal"'],
      default: '"default"',
      description: "The visual style variant of the input.",
    },
    size: {
      type: ['"xs"', '"sm"', '"md"', '"lg"'],
      default: '"md"',
      description: "The size of the input.",
    },
    invalid: {
      type: "boolean",
      description: "Whether the input is in an invalid state.",
    },
  }}
/>

### Input.Group

Extends the `div` element. Wraps an `Input` together with one or more `Input.Addon` siblings — useful for icon prefixes, text suffixes, password toggles, country pickers, etc.

The group owns the visual frame: border, height, focus ring, invalid/disabled state. Pass `size` and `variant` here, not on the inner `Input` — sizing inside the group is inherited from this context, so the inner `Input`'s own `size`/`variant` props are ignored.

<PropsTable
  definition={{
    size: {
      type: ['"xs"', '"sm"', '"md"', '"lg"'],
      default: '"md"',
    },
    variant: {
      type: ['"default"', '"minimal"'],
      default: '"default"',
    },
  }}
/>

### Input.Addon

Extends the `div` element. Renders flush at the group's edges with size-aware padding inherited from `Input.Group`. Drop interactive content (a `<button>`, a `Tooltip.Trigger`, a `Popover.Trigger`) directly inside — no `pointer-events-auto` ceremony needed.

Static addons (icons, text) are click-through to the input — clicking a leading lock icon focuses the input the way you'd expect. The addon detects whether the click target is interactive (`button`, `a`, `input`, `[role="button"]`, etc.) and only forwards focus when it isn't, so interactive content keeps its own click semantics.

For component-as-addon composition (a popover trigger, a country picker, etc.), pass `asChild` and provide your own element. The addon's flex/sizing/padding classes are merged onto the child.

<PropsTable
  definition={{
    asChild: {
      type: "boolean",
    },
  }}
/>

## Examples

### Simple

Basic usage of the input component.

<Preview slug="input" />

### Minimal

A more subtle variant with minimal styling.

<Preview slug="input-minimal" />

### Sizes

<Preview slug="input-sizes" />

### Disabled

Example of a disabled input state. Inside a group, the disabled visual cascades to the wrapper via `:has(input:disabled)`, so addons dim with the input.

<Preview slug="input-disabled" />

<Preview slug="input-group-disabled" />

### Invalid

Showing validation state with an invalid input. Inside a group, the error border and ring carry across via `:has([data-invalid])` — works the same whether `invalid` is set on the inner `Input` or `aria-invalid` is injected by `Field.Control`.

<Preview slug="input-invalid" />

<Preview slug="input-group-invalid" />

### Icon

Input with an icon prefix.

<Preview slug="input-icon" />

### Text Addons

Using text prefixes and suffixes.

<Preview slug="input-text-addons" />

### Icon and Action

Combining an icon prefix with an interactive suffix.

<Preview slug="input-icon-action" />

### Interactive Addon

Example with clickable addon elements.

<Preview slug="input-interactive-addon" />

### Component as Addon

Composing a `Menu.Trigger` (or any other compound primitive) as an addon via `asChild`. Demonstrates a phone field with a country picker — the trigger lives inside the input frame, the menu floats from it with keyboard nav and focus management.

<Preview slug="input-component-addon" />

### File

The native `::file-selector-button` is restyled to read as inline text inside the input frame — no native border, background, or chunky chrome.

<Preview slug="input-file" />

### Date & Time

The browser's calendar/clock picker icon is suppressed in Chromium and absent by default in Safari, so `date`, `time`, `datetime-local`, `month`, and `week` render as a clean text frame in both. Firefox keeps its native calendar button on `date` / `datetime-local` / `month` / `week` (it doesn't expose a pseudo-element to hide it) — reach for [`DatePicker`](/ui/date-picker) when you need a unified picker UI across browsers.

<Preview slug="input-date-time" />

## Best Practices

1. **Validation**:
   - Provide clear error messages
   - Use appropriate input types

2. **Addons**:
   - Keep icons and text addons relevant
   - Ensure interactive elements are clearly clickable
   - Maintain consistent spacing
