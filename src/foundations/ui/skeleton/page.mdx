export const metadata = {
  title: "Skeleton",
  description:
    "A component to display a replacement for content while loading.",
  files: ["src/foundations/ui/skeleton/skeleton.tsx"],
  preview: "skeleton",
};

## API Reference

Extends the `div` element.
