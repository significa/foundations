---
title: Table
description: Semantic table primitive. Compose with TanStack Table for sorting, selection, filtering, and pagination.
preview: table
files:
  - src/foundations/ui/table/table.tsx
dependencies:
  - name: "@phosphor-icons/react"
    href: https://phosphoricons.com

folder: UI
---

## Anatomy

```tsx
<Table.Container>
  <Table>
    <Table.Caption />
    <Table.Header>
      <Table.Row>
        <Table.Head />
        <Table.SortableHead sort={false} onSort={() => {}} />
      </Table.Row>
    </Table.Header>
    <Table.Body>
      <Table.Row>
        <Table.Cell />
      </Table.Row>
    </Table.Body>
    <Table.Footer />
  </Table>
</Table.Container>
```

The compound parts are purely structural — they map one-to-one onto the underlying HTML table elements (`<table>`, `<thead>`, `<tbody>`, `<tfoot>`, `<tr>`, `<th>`, `<td>`, `<caption>`). State for sorting, selection, filtering, and pagination lives with the consumer. For dashboards with non-trivial data, compose with [TanStack Table](https://tanstack.com/table/v8) — see the recipe below.

`Table` renders the bare `<table>` element. Wrap it in `Table.Container` when you want a bordered card, horizontal overflow on narrow viewports, or a sticky header — the container is the scroll context, so it owns the border, the rounding, and any height constraint. A plain `<Table>` with no container is perfectly valid for a table that should flow inline with the page.

## API Reference

### Table.Container

Optional `div` wrapper that becomes the table's scroll container — it handles horizontal overflow and, with a `max-h-*`, vertical scrolling for a sticky header. Pin a height **here**, not on an outer element, since `position: sticky` engages against the nearest scroll container. Extends the `div` element.

<PropsTable
  definition={{
    bordered: {
      default: "true",
      type: "boolean",
      description: "Wrap the table in a bordered card (rounded border + subtle shadow).",
    },
  }}
/>

### Table

Extends the `table` element. Renders the bare `<table>` — wrap it in `Table.Container` for borders, overflow, or sticky headers.

<PropsTable
  definition={{
    density: {
      default: '"md"',
      type: ['"sm"', '"md"'],
      description: "Vertical padding of body cells.",
    },
  }}
/>

### Table.Header

Extends the `thead` element.

<PropsTable
  definition={{
    sticky: {
      type: "boolean",
      description:
        "Pin the header to the top of the nearest scroll container. Requires the container to have a defined height for `position: sticky` to engage.",
    },
  }}
/>

### Table.Body

Extends the `tbody` element. Adds bottom borders between rows.

### Table.Footer

Extends the `tfoot` element. Use for totals or summary rows.

### Table.Row

Extends the `tr` element. Set `data-selected` on the row to apply the selected style — the data-table recipe wires this up automatically.

### Table.Head

Extends the `th` element.

<PropsTable
  definition={{
    align: {
      default: '"start"',
      type: ['"start"', '"center"', '"end"'],
      description: "Text alignment. Use 'end' for numeric columns.",
    },
  }}
/>

### Table.Cell

Extends the `td` element.

<PropsTable
  definition={{
    align: {
      default: '"start"',
      type: ['"start"', '"center"', '"end"'],
    },
  }}
/>

### Table.Caption

Extends the `caption` element. Renders below the table by default (`caption-side: bottom`).

### Table.SortableHead

Renders a `<th>` containing a sort button with caret affordance and `aria-sort` wiring. State is fully controlled — pass the current direction and handle the cycle yourself.

<PropsTable
  definition={{
    sort: {
      type: ['"asc"', '"desc"', "false"],
      description: "Current sort direction. `false` means unsorted.",
    },
    onSort: {
      type: "() => void",
      description:
        "Called when the header is activated. Implement the unsorted → asc → desc cycle in the parent.",
    },
    align: {
      default: '"start"',
      type: ['"start"', '"center"', '"end"'],
    },
  }}
/>

## Examples

### Basic

A static table with a footer summary.

<Preview slug="table" />

### Sortable headers

Local state drives the sort direction. The header cycles `asc → desc` on each click.

<Preview slug="table-sortable" />

### Row selection

Header checkbox toggles all rows; selected rows get a `data-selected` attribute that the row style picks up.

<Preview slug="table-with-selection" />

### Sticky header

Wrap the table in `Table.Container` with a `max-h-*` and set `sticky` on `Table.Header`. The container is the scroll context, so the header pins to its top as the body scrolls.

<Preview slug="table-sticky" />

### Data table (with TanStack Table)

For sorting, filtering, selection, and pagination together, compose `Table` with [`@tanstack/react-table`](https://tanstack.com/table/v8). This is **not** a hard dependency of the primitive — you only need it when you want the engine.

```bash
pnpm add @tanstack/react-table
```

The recipe:

1. Define `ColumnDef<T>[]` — `accessorKey`, `header`, `cell`, optional `meta` for cell alignment.
2. Call `useReactTable` with the row models you need (`getCoreRowModel`, `getSortedRowModel`, `getPaginationRowModel`).
3. Map `table.getHeaderGroups()` into `Table.Head` / `Table.SortableHead` based on `column.getCanSort()`.
4. Map `table.getRowModel().rows` into `Table.Row` / `Table.Cell`. Use `flexRender(cell.column.columnDef.cell, cell.getContext())` for the cell body.
5. Wire `Pagination` to `table.getPageCount()`, `table.previousPage()`, and `table.nextPage()`.

<Preview slug="table-data-table" />

## Best Practices

1. **Scope**:
   - This primitive is the styled `<table>` — not a data engine. For ≤1k rows in a dashboard, compose with TanStack Table as shown above.
   - For 100k+ rows you'll want virtualization. Reach for `@tanstack/react-virtual` on top of TanStack Table, or switch to a dedicated data grid (AG Grid, LyteNyte, MUI X DataGrid Pro).
   - Out of scope here: column resize/reorder, pinning, grouping, range selection, inline editing. If you need them, compose them at the call site or pick a heavier tool.

2. **Alignment**:
   - Use `align="end"` on numeric columns and their header. It makes magnitudes scannable.
   - Keep text columns left-aligned. Centered text in tables is rarely the right choice.

3. **Accessibility**:
   - Use `<Table.Caption>` to describe the table's purpose — it's announced by screen readers.
   - `Table.SortableHead` sets `aria-sort` automatically; don't override it.
   - When using checkboxes for selection, set `aria-label` on each — column headers alone aren't enough context.
   - For a row that acts as a header for its row (e.g. a name column in a record list), you can render `<Table.Head scope="row">` inside `Table.Body`.

4. **Sticky headers**:
   - `sticky` on `Table.Header` pins to the nearest scroll container, which must have a bounded height for `position: sticky` to engage. Wrap the table in `Table.Container` and put the `max-h-*` on the container itself — not on an outer element, which would scroll the whole block (header included) instead of just the body.

5. **Empty and loading states**:
   - Render a single full-width row with `colSpan={columns.length}` for "No results."
   - For loading, replace cell contents with `<Skeleton />` rather than swapping the table for a spinner — preserves layout and prevents jumpy reflows.
