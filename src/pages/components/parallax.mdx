import { ComponentPreview } from '@/components/ComponentPreview';
import { FileSource } from '@/components/FileSource';
import { Box } from '@/components/Box';
import { Parallax } from '@/foundations/components/Parallax';

# Parallax

A component that creates an scroll-based parallax effect. The parallax translation is applied in such a way that the element always hits its original (non-translated) position when its reaches the center of the viewport.
On touch devices and when there is a reduce motion preference, the translation is automatically removed.

<ComponentPreview
  path="@/foundations/components/Parallax/examples/ParallaxExample"
  inline={false}
  withSource={false}
/>

## Install

<FileSource path="@/foundations/components/Parallax" />

### Dependencies

- [utils/useIsMounted](/hooks/use-is-mounted)
- [utils/useIsTouchscreen](/hooks/use-is-touchscreen)
- [utils/useViewport](/hooks/use-viewport)

```bash copy
npm install framer-motion
```

## API Reference

| Prop    |   Type   | Default Value |
| :------ | :------: | ------------: |
| `speed` | `number` |             - |

## Usage

```tsx copy
import { Parallax } from '[path]/Parallax';
```

```tsx copy
<Parallax speed={1.2}>
    <!-- Content --->
</Parallax>
```

## Examples

### Speed Control

<ComponentPreview path="@/foundations/components/Parallax/examples/ParallaxSpeeds" inline={false} />

### Column Stagger

<ComponentPreview
  path="@/foundations/components/Parallax/examples/ParallaxColumnStagger"
  inline={false}
/>
